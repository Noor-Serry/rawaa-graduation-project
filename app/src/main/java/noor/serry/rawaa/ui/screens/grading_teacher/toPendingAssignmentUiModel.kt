package noor.serry.rawaa.ui.screens.grading_teacher// FILE DELETED — do not restore without a matching backend endpoint.
//
// This mapper imported DoctorAssignmentDto and PendingAssignmentUiModel,
// neither of which exists in ApiDtos.kt / GradingUiState.kt.
//
// DoctorDashboardDto (GET /api/doctor/dashboard) has no `assignments` field.
// AssignmentGradingCard.kt — the only consumer — is already commented out.
//
// Remove this file from the project. It will not compile.
